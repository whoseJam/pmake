import { SDNode } from "SD/Node/SDNode";

export class Cast {
    static castToSDNode(parent: SDNode, any: any);
    static castToSDNode(parent: SDNode, any: any, id: number);
    static castD3ToNake(d3: any);
}