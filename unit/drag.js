import * as sd from "@/sd";

const svg = sd.svg();
const div = sd.div();
const circle = new sd.Circle(svg).drag(true);
const button = new sd.Button(div);

sd.init(() => {});

sd.main(async () => {});
