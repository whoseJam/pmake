```javascript
const svg = sd.svg(); // 获取画布
const C = sd.color();
const D = sd.device();
const I = sd.input();
const L = sd.layout(); // 获取模块

class MyComp1 extends sd.Group {
    ...
}

class MyComp2 extends sd.Group {
    ...
}

const comp1 = new MyComp1(svg);
const comp2 = new MyComp2(svg);

sd.init(() => {
    comp1.x(100).y(100);
    comp2.x(200).y(100);
});

sd.main(async () => {
    await sd.pause();
    // 动画代码
})
```

使用 const 优先的变量定义方式，所有变量，能定义为 const，就定义为 const
