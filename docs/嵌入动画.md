# 嵌入动画

嵌入 PPT 的动画需遵循“零说明文字”原则：所有解释性内容移至 PPT，动画仅呈现符号化数据（如 `dis[u]`、`d`、`f`）。

## 语言与字幕规范

-   禁止出现中文及完整英文句子
-   禁止使用 `sd.Caption`
-   仅允许符号化文本
-   说明性内容由 PPT 承载
-   `sd.pause()` 不传参，强制手动触发，步进节奏与演讲同步

当你想要使用更复杂的结构的时候，你可以自定义组件，组件里面一定要实现响应式布局，管理好 x,y,width 和 height

关于自定义组件的详情请参考 ./动画框架高级功能.md
