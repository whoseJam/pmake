以前的动画使用了大量的复杂的组件，组件包括了元素管理和元素布局两个职责：

比如：

元素管理：图组件会在内部自动管理连边的创建、节点的创建

元素布局：树组件根据树的形态，决定每个节点应该在什么位置

但现在组件已经被移除了，元素布局功能被保留下来，移动到了 sd.layout() 模块下

元素管理不存在了，你需要自己进行元素管理

最好自定义组件，去替换原来的组件

请完成旧的动画代码，到新的动画代码的迁移
